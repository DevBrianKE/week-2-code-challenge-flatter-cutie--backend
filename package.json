{
  "name": "week-2-code-challenge-backend",
  "version": "1.0.0",
  "description": "A simple backend for the week-2-code-challenge using JSON Server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
